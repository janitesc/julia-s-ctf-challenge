currently, cmgr playtest and solve script work for a very rudimentary "trojan horse/evil process" memdump challenge with a dynamic flag, that sets up a different memdump/flag each time

the full challenge goal is to have multiple pids/files so it's actually fun to solve, and maybe a "suspicious" pid that's deleting the file that the use has to figure out. I also want to potentially encrypt the files.
